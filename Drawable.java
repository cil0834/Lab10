import java.awt.Graphics;
/**
 * The drawable interface whose only method is the draw method used to determine how to draw a shape
 * @author Caleb
 *
 */
public interface Drawable {
	
	
	public void draw(Graphics g);
}
